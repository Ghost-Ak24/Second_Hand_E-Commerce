import { useEffect, useState } from "react"; import { api } from "../lib/api";import ProductCard from"../components/ProductCard"; import Loader from"../components/Loader";
export default function Wishlist() { const [ps, setPs] = useState([]), [loading, setLoading] = useState(true); async function load() { setLoading(true); try { const ids = await api.getWishlist(); setPs((await Promise.all(ids.map(id => api.getProduct(id).catch(() => null)))).filter(Boolean)) } finally { setLoading(false) } } useEffect(() => { load() }, []); if (loading) return <Loader />; return <div className="container-page py-10"><p className="text-sm font-bold text-indigo-600">Saved for later</p><h1 className="mt-1 text-3xl font-black">Wishlist</h1>{ps.length ? <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{ps.map(p => <ProductCard key={p.id} product={p} wished onWishChange={load} />)}</div> : <div className="mt-10 rounded-3xl border border-dashed bg-white py-20 text-center"><b>Your wishlist is empty</b></div>}</div> }